# MySQL 이미지를 기반으로 도커 이미지 생성
FROM mysql:latest

# MySQL 설정
ENV MYSQL_ROOT_PASSWORD=1234
ENV MYSQL_DATABASE=edudb
ENV MYSQL_USER=jdbctest
ENV MYSQL_PASSWORD=jdbctest

# 포트 설정 (기본 MySQL 포트는 3306)
EXPOSE 3306
